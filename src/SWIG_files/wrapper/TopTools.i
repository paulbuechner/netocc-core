// SPDX-FileCopyrightText: 2026 Paul Büchner
// SPDX-License-Identifier: MIT

/* TopTools: generated by netocc-gen from OCCT 8.0.1. Don't edit; change the generator or its config. */
%module TopToolsModule

%include "NetOcc.i"

%{
#include <TopTools_module.hxx>
%}

%import "TopAbs.i"
%import "Standard.i"
%import "gp.i"
%import "TopLoc.i"
%import "TopoDS.i"
%netocc_csimports(using OCC.Core.TopAbs; using OCC.Core.Standard; using OCC.Core.gp; using OCC.Core.TopLoc; using OCC.Core.TopoDS;)

%occt_valueclass(TopTools_LocationSet)
%occt_valueclass(TopTools_ShapeSet)

enum TopTools_FormatVersion {
  TopTools_FormatVersion_VERSION_1 = 1,
  TopTools_FormatVersion_VERSION_2 = 2,
  TopTools_FormatVersion_VERSION_3 = 3,
  TopTools_FormatVersion_CURRENT = 3,
};

class TopTools {
public:
  static void Dummy(int I);
};

class TopTools_LocationSet {
public:
  TopTools_LocationSet();
  void Clear();
  int Add(const TopLoc_Location& L);
  const TopLoc_Location& Location(int I) const;
  int Index(const TopLoc_Location& L) const;
};

class TopTools_ShapeMapHasher {
public:
};

class TopTools_ShapeSet {
public:
  TopTools_ShapeSet();
  void SetFormatNb(int theFormatNb);
  int FormatNb() const;
  void Clear();
  int Add(const TopoDS_Shape& S);
  const TopoDS_Shape& Shape(int I) const;
  int Index(const TopoDS_Shape& S) const;
  const TopTools_LocationSet& Locations() const;
  void DumpExtent(TCollection_AsciiString& S) const;
  void AddGeometry(const TopoDS_Shape& S);
  void AddShapes(TopoDS_Shape& S1, const TopoDS_Shape& S2);
  void Check(TopAbs_ShapeEnum T, TopoDS_Shape& S);
  int NbShapes() const;
};

%occt_list(TopTools_ListOfShape, TopoDS_Shape)
%occt_sequence(TopTools_SequenceOfShape, TopoDS_Shape)
