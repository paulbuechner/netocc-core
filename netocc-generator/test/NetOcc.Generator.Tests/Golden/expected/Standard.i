// SPDX-FileCopyrightText: 2026 Paul Büchner
// SPDX-License-Identifier: MIT

/* Standard: generated by netocc-gen from OCCT 8.0.1. Don't edit; change the generator or its config. */
%module StandardModule

%include "NetOcc.i"

%{
#include <Standard_module.hxx>
%}

%netocc_csimports()

%occt_valuetype(Standard_UUID)
%occt_valueclass(Standard_Failure)
%occt_handle(Standard_Transient, Standard_Transient)

%{
// Layout guards for the C# structs in src/NetOcc/Standard/: a mismatch fails the native build.
static_assert(sizeof(Standard_UUID) == 16 && alignof(Standard_UUID) == 4 && std::is_trivially_copyable<Standard_UUID>::value, "Standard_UUID layout");
%}

%inline %{
// sizes for the managed layout tests
inline int NetOcc_SizeOf_Standard_UUID() { return (int)sizeof(Standard_UUID); }
%}

class Standard_Failure {
public:
  Standard_Failure();
  const char* what() const;
};

class Standard_Transient {
public:
  Standard_Transient();
  int GetRefCount() const;
};

// the native members of the C# structs (src/NetOcc/Standard/*.g.cs)
%inline %{
inline void NetOcc_Standard_UUID_New(Standard_UUID& theSelf) { theSelf = Standard_UUID(); }
%}
